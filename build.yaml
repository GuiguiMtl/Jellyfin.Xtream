---
name: "Jellyfin Xtream"
guid: "5d774c35-8567-46d3-a950-9bb8227a0c5d"
version: "0.9.5.0"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Stream content from an Xtream-compatible server."
description: >
  Stream Live IPTV, Video On-Demand, and Series from an Xtream-compatible server using this plugin.
category: "LiveTV"
owner: "Kevinjil"
artifacts:
  - "Jellyfin.Xtream.dll"
changelog: |
  Fix EPG timezone offset ignored in program guide:
  - Parse timezone-aware EPG start/end strings (ISO 8601 and XMLTV format, e.g. "20240115180000 +0200")
  - Prefer the offset-aware string field over the Unix timestamp when the provider includes an explicit timezone
  - Fall back to the Unix timestamp for providers that omit timezone info (no behaviour change for those)
